FLOWMATRIX_MODE=lan
DATABASE_URL=file:./data/flowmatrix.sqlite
FLOWMATRIX_STORAGE_ROOT=./data/results

# Online provider baseURL, model, and API key are saved from the settings page.
# Set this before cloud mode stores encrypted provider secrets.
PROVIDER_ENCRYPTION_KEY=
